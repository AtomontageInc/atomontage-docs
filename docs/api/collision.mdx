---
title: Collision
id: Collision
---
`Client`
`Server`

:::warning Deprecated
The collision functions in this class are old and may not correctly work.
Instead use `Cast:Ray()` and `Collide()`
:::

## Constructors

### Collision Collision() {#Collision-Collision}

## Methods

### Hit[] Raycast() {#Hit-Raycast}
Use rayPos, rayDir, returns table of Hit values or empty table for no hit.
Returns [Hit](Hit).

### Hit[] Raycast(boolean readColor, boolean readNormal, boolean readMaterial) {#Hit-Raycast-boolean-readColor-boolean-readNormal-boolean-readMaterial}

### Hit[] Raycast(Vec3 pos, Vec3 dir) {#Hit-Raycast-Vec3-pos-Vec3-dir}

### Hit[] Raycast(Vec3 pos, Vec3 dir, boolean? readColor, boolean? readNormal, boolean? readMaterial) {#Hit-Raycast-Vec3-pos-Vec3-dir-boolean-readColor-boolean-readNormal-boolean-readMaterial}

### table RaycastTo(Vec3 startPos, Vec3 endPos, boolean? readColor, boolean? readNormal, boolean? readMaterial) {#table-RaycastTo-Vec3-startPos-Vec3-endPos-boolean-readColor-boolean-readNormal-boolean-readMaterial}

### Overlap[] GetOverlap(Shape? shape) {#Overlap-GetOverlap-Shape-shape}
Checks collision between shape and geometry passed by filter.
Returns [Overlap](Overlap).

## Properties

### Filter filter {#Filter-filter}

### integer maxHitCount {#integer-maxHitCount}
Doesn't influence result, just limits returned hits table size

### Shape shape {#Shape-shape}

### boolean precise {#boolean-precise}

### Vec3 rayPos {#Vec3-rayPos}

### Vec3 rayDir {#Vec3-rayDir}

