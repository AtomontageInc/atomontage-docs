---
title: VoxelData
id: VoxelData
---
`Client`
`Server`

Component that holds voxel data. The actual data itself is in the `data` property.
The data will only render if the object also has a `VoxelRender` component.

## Metamethods

### boolean __eq(VoxelData a, VoxelData b) {#boolean-eq-VoxelData-a-VoxelData-b}

## Methods

### nil Mirror(Vec3 worldPos, Vec3 worldNormal) {#nil-Mirror-Vec3-worldPos-Vec3-worldNormal}

### Vec3 center, Vec3 size GetAABounds() {#Vec3-center-Vec3-size-GetAABounds}
Returns the world-space axis-aligned bounds (center, size). Affected by the object's rotation, and reports size `(0,0,0)` when `receiveTransform=false`.

### Vec3 center, Quat rot, Vec3 size GetBounds() {#Vec3-center-Quat-rot-Vec3-size-GetBounds}

### boolean success, string usedPath SetPath(string path) {#boolean-success-string-usedPath-SetPath-string-path}

## Properties

### boolean active {#boolean-active}

### boolean activeInHierarchy {#boolean-activeInHierarchy}

### Object object {#Object-object}

### Object obj {#Object-obj}

### boolean isDestroyed {#boolean-isDestroyed}

### string type {#string-type}

### Vec3 size {#Vec3-size}

### integer contentVersion {#integer-contentVersion}
Monotonic counter incremented every time the underlying voxel content is invalidated (any edit that changes voxel presence). Cheap O(1) read for caching downstream computations like total volume.

### string path {#string-path}

### string originalPath {#string-originalPath}

### VoxelDataResource data {#VoxelDataResource-data}
The voxel data resource that this voxel data is using

### boolean save {#boolean-save}
Marks the voxel data to be persisted on save; it does not write any voxels by itself.

### boolean editable {#boolean-editable}

